# Findings — <Unit name>

> Companion to `docs/map/<unit>.md`. One finding per block. Newest issues at the top of their
> severity tier. When a finding is fixed, move it to the "Resolved" section with the commit sha.

Severity and category legend lives in [README.md](./README.md). Quick reminder:
**P0** data-integrity/broken · **P1** EGRESS (primary goal) · **P2** perf/security · **P3** cleanup/tests.

---

## Open

### <UNIT>-01 · P0 · ATOMICITY · `services/db/<file>.ts:NN`
- **Problem:** <what the code does that is wrong/risky — be specific, name functions/lines>
- **Impact:** <what breaks for the user / the data / the bill, and when>
- **Fix:** <the concrete change. If it needs a migration/RPC, say so.>

<!-- copy the block above for each finding. ID = <UNIT>-NN, monotonically increasing, never reused. -->

---

## Resolved
<!-- - <UNIT>-NN · fixed in <sha> <date> — one line on the fix. -->
